import { Component } from '@angular/core';
import { Router, ActivatedRoute, Params } from '@angular/router';

@Component({
  selector: 'contact-app',
  template: '<contact-form-app>Loading...</contact-form-app>'
})

export class ContactComponent {}
